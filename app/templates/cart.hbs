{{page-title "Cart"}}
<main class="container mt-3">
  <h1>Your cart</h1>
  <nav aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item active" aria-current="page">
        <LinkTo @route="index">
          Home
        </LinkTo>
      </li>
      <li class="breadcrumb-item">
        <LinkTo @route="cart">
          Shopping cart
        </LinkTo>
      </li>
    </ol>
  </nav>
  <hr>
  <p> {{this.model}}</p>
  <section class="w-50 ms-auto text-end mb-5">
    <div class="row">
      <span class="col">Subtotal</span>
      <span class="col">{{this.subtotal}}</span>
    </div>
    <div class="row">
      <span class="col">Total</span>
      <span class="col">{{this.total}}</span>
    </div>
  </section>
  <button class="btn btn-success ms-auto float-end" type="button">
    Check out
  </button>
  <LinkTo @route="index">
    <button class="btn btn-primary ms-auto me-2 float-end" type="button">
      Continue shopping
    </button>
  </LinkTo>

  {{!-- Partial for when empty --}}
  {{!-- <section class="w-50 mb-5 d-flex flex-column align-items-center"> --}}
    {{!-- Make this into a partial --}}
    {{!-- <p>Your shopping cart is empty.</p> --}}
    {{!-- <i class="fa-regular fa-face-sad-cry fs-1"></i> --}}

    {{!-- <LinkTo @route="index">
      <button class="btn btn-primary mt-3">
        Continue shopping
      </button>
    </LinkTo> --}}
  {{!-- </section>
   --}}
</main>
{{outlet}}
